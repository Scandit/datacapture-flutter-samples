name: TextCaptureSample
description: Uses Text Capture to recognize various kinds of texts in the specific locations in the frame.

publish_to: 'none'

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  scandit_flutter_datacapture_text:
    '>=6.15.1 <6.15.2'
  scandit_flutter_datacapture_parser:
    '>=6.15.1 <6.15.2'
  permission_handler: ">=9.0.1 <=9.0.2"
  flutter_platform_widgets: ^1.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
